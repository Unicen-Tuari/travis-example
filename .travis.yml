dist: trusty
sudo: required
language: php

php:
  - 7.1.3
  - 7.2
  - nightly
services:
    -postgresql
install:
  - composer install
before_script:
  - cp .env.example .env
  - psql -c 'create database travis_ci_test;' -U postgres
  - cp config/database.yml.travis config/database.yml 
script:
  - vendor/bin/phpunit
