sudo: false
language: php

php:
  - '7.1.3'
  - '7.2'
  - nightly

services:
  - postgresql

before_script:
  - psql -c 'create database unicenci;' -U postgres
  - composer install
  - cp .env.example .env
  - php artisan migrate

script: phpunit --configuration phpunit.xml
